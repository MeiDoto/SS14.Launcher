<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SS14.Launcher.ViewModels"
        xmlns:loc="clr-namespace:SS14.Launcher.Localization"
        mc:Ignorable="d" d:DesignWidth="620" d:DesignHeight="480"
        MinWidth="560" MinHeight="420"
        Width="620" Height="480"
        x:Class="SS14.Launcher.Views.AccountInfoDialog"
        x:DataType="vm:AccountInfoViewModel"
        Icon="/Assets/icon.ico"
        Title="{loc:Loc account-info-title}"
        Padding="12"
        Background="{DynamicResource ThemePopupBackgroundBrush}"
        WindowStartupLocation="CenterOwner">
  <Design.DataContext>
    <vm:AccountInfoViewModel />
  </Design.DataContext>
  <Window.DataContext>
    <vm:AccountInfoViewModel />
  </Window.DataContext>

  <DockPanel>
    <!-- Bottom Close Button -->
    <DockPanel Height="36" Margin="0 10 0 0" DockPanel.Dock="Bottom" LastChildFill="False">
      <Button DockPanel.Dock="Right" Click="CloseClicked" Content="{loc:Loc account-info-close}" Classes="OpenLeft" />
      <Button DockPanel.Dock="Right" Command="{Binding OpenAccountWebsite}" Content="{loc:Loc account-info-open-web}" Classes="OpenRight" />
    </DockPanel>

    <ScrollViewer DockPanel.Dock="Top" HorizontalScrollBarVisibility="Disabled">
      <StackPanel Spacing="10" Margin="4">

        <!-- CARD 1: ACCOUNT DETAILS -->
        <Border Classes="Card">
          <StackPanel Spacing="8">
            <TextBlock Text="{loc:Loc account-info-section-profile}" Classes="SectionTitle" />

            <!-- Username -->
            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-username}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding Username}" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource ThemeNanoGoldBrush}" VerticalAlignment="Center" />
            </Grid>

            <!-- User ID / GUID -->
            <Grid ColumnDefinitions="140, *, Auto" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-user-id}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBox Grid.Column="1" Text="{Binding UserId}" IsReadOnly="True" VerticalAlignment="Center" Margin="0, 0, 4, 0" />
              <Button Grid.Column="2" Content="{loc:Loc account-info-copy}" Command="{Binding CopyUserId}" ToolTip.Tip="{loc:Loc account-info-copy-id-tooltip}" />
            </Grid>

            <!-- Email -->
            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-email}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding Email}" VerticalAlignment="Center" />
            </Grid>

            <!-- Status -->
            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-session-status}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding StatusText}" Foreground="#5BD969" VerticalAlignment="Center" />
            </Grid>

            <!-- Total Playtime -->
            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-total-playtime}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding TotalPlaytimeText}" Foreground="{DynamicResource ThemeNanoGoldBrush}" FontWeight="SemiBold" VerticalAlignment="Center" />
            </Grid>
          </StackPanel>
        </Border>

        <!-- CARD 2: SECURITY & PASSWORD -->
        <Border Classes="Card">
          <StackPanel Spacing="8">
            <TextBlock Text="{loc:Loc account-info-section-security}" Classes="SectionTitle" />

            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-password-storage}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding PasswordStatusText}" VerticalAlignment="Center" />
            </Grid>

            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-token-expires}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding TokenExpiresText}" VerticalAlignment="Center" />
            </Grid>

            <WrapPanel Orientation="Horizontal" Margin="0, 6, 0, 0">
              <Button Content="{loc:Loc account-info-change-password}" Command="{Binding OpenChangePassword}" Margin="0, 0, 8, 4" />
              <Button Content="{loc:Loc account-info-2fa}" Command="{Binding Open2FaSettings}" Margin="0, 0, 8, 4" />
            </WrapPanel>
          </StackPanel>
        </Border>

        <!-- CARD 3: DEVICE & HARDWARE ID -->
        <Border Classes="Card">
          <StackPanel Spacing="8">
            <TextBlock Text="{loc:Loc account-info-section-device}" Classes="SectionTitle" />

            <Grid ColumnDefinitions="140, *, Auto" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-hwid}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBox Grid.Column="1" Text="{Binding Hwid}" IsReadOnly="True" VerticalAlignment="Center" Margin="0, 0, 4, 0" />
              <Button Grid.Column="2" Content="{loc:Loc account-info-copy}" Command="{Binding CopyHwid}" ToolTip.Tip="{loc:Loc account-info-copy-hwid-tooltip}" />
            </Grid>

            <Grid ColumnDefinitions="140, *" Margin="0, 2">
              <TextBlock Grid.Column="0" Text="{loc:Loc account-info-system}" VerticalAlignment="Center" Foreground="{DynamicResource ThemeSubTextBrush}" />
              <TextBlock Grid.Column="1" Text="{Binding SystemInfo}" VerticalAlignment="Center" />
            </Grid>

            <Button Content="{loc:Loc account-info-copy-all-diag}" Command="{Binding CopyAllDiagnostics}" Margin="0, 6, 0, 0" HorizontalAlignment="Left" />
          </StackPanel>
        </Border>

      </StackPanel>
    </ScrollViewer>
  </DockPanel>
</Window>
